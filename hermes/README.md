# Hermes Agent

Place the Hermes Agent application source and Dockerfile in this directory.
The root `docker-compose.yml` builds this service locally as `hermes-agent:local`.

This directory is intentionally scaffolded only; no image is built or started
by the repository bootstrap step.
